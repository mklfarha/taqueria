# Code generated by nuzur go-code-gen. DO NOT EDIT.
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taqueria.fullname" . }}-test-connection"
  labels:
    {{- include "taqueria.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: http-check
      image: busybox
      command: ['wget', '-q', '-O-']
      args: ['{{ include "taqueria.fullname" . }}:{{ .Values.service.httpPort }}/healthz']
  restartPolicy: Never
