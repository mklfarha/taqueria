# nuzur deploy — keep secrets and build artifacts out of git
config/prod.yaml
*.local.yaml
.env
.env.*
*.key
*.pem
*.p12
/bin/
*.exe
.DS_Store

# nuzur deploy state — regenerated on every deploy, contains host/user details
.nuzur/
